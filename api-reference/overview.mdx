---
title: "API Reference"
description: "Complete API documentation for the ShapeShift cross-chain swap infrastructure."
---

## Authentication

The API uses key-based authentication. Pass your API key via the `X-API-Key` header. For EIP-7702 mode, you sign authorizations with your wallet. For non-7702 modes, you sign or submit transactions directly.

```bash
curl -X GET "https://api.shapeshift.com/v1/chains" \
  -H "X-API-Key: your-api-key"
```

## Response Format

All API responses follow this format:

```json
{
  "success": boolean,
  "data": object | null,
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable error description"
  }
}
```

## Error Handling

| Status | Code | Description |
|--------|------|-------------|
| 200/201 | — | Success |
| 400 | `INVALID_PARAMS` | Invalid or missing request parameters |
| 401 | `UNAUTHORIZED` | Invalid or missing API key |
| 403 | `FORBIDDEN` | API key lacks required permissions |
| 404 | `NOT_FOUND` | Resource not found |
| 429 | `RATE_LIMITED` | Too many requests |
| 500 | `INTERNAL_ERROR` | Unexpected server error |

## Endpoints Overview

### Chains

Query supported blockchains and their metadata.

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/v1/chains` | List all supported blockchains |
| `GET` | `/v1/chains/count` | Get total chain count |

### Assets

Browse and search supported assets using CAIP-19 identifiers.

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/v1/assets` | List supported assets with filtering and pagination |
| `GET` | `/v1/assets/{assetId}` | Get detailed info about a specific asset |

### Swaps

Get real-time swap rates and generate optimized quotes.

| Method | Endpoint | Description |
|--------|----------|-------------|
| `GET` | `/v1/swap/rates` | Get current swap rates between asset pairs |
| `POST` | `/v1/swap/quote` | Generate an optimized swap quote |
