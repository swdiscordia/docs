---
title: "Quick Start"
sidebarTitle: "Quick Start"
description: "Get up and running with the ShapeShift API in under 5 minutes."
icon: "rocket"
---

## Prerequisites

Before you begin, make sure you have:

<Steps>
  <Step title="An API Key">
    Request one from the [ShapeShift developer portal](https://shapeshift.com/developers).
  </Step>
  <Step title="Node.js 18+ (optional)">
    Required if using the JavaScript/TypeScript SDK. Otherwise, any HTTP client works.
  </Step>
</Steps>

## Your First API Call

Let's start by fetching the list of supported blockchains:

<CodeGroup>
```bash cURL
curl -X GET "https://api.shapeshift.com/v1/chains" \
  -H "Content-Type: application/json"
```

```python Python
import requests

response = requests.get(
    "https://api.shapeshift.com/v1/chains",
    headers={
        "Content-Type": "application/json"
    }
)

data = response.json()
print(data)
```

```javascript JavaScript
const response = await fetch("https://api.shapeshift.com/v1/chains", {
  headers: {
    "Content-Type": "application/json",
  },
});

const data = await response.json();
console.log(data);
```
</CodeGroup>

### Example Response

```json
{
  "chains": [
    {
      "chainId": "eip155:1",
      "name": "Ethereum",
      "type": "evm",
      "symbol": "ETH",
      "precision": 18,
      "color": "#5C6BC0",
      "icon": "https://rawcdn.githack.com/trustwallet/assets/.../ethereum/info/logo.png",
      "explorer": "https://etherscan.io",
      "explorerAddressLink": "https://etherscan.io/address/",
      "explorerTxLink": "https://etherscan.io/tx/",
      "nativeAssetId": "eip155:1/slip44:60"
    }
  ],
  "timestamp": 1700000000
}
```

<Info>
  Only **swap endpoints** (`/v1/swap/rates` and `/v1/swap/quote`) require the \`X-API-Key\` header for authentication. Chains and assets endpoints are publicly accessible. See the [Authentication](/getting-started/authentication) section for details.
</Info>

## Execute a Swap

Now let's do something more interesting — get a swap quote:

<Steps>
  <Step title="Get rates">
    First, compare rates across aggregators:
    ```bash
    curl -X GET "https://api.shapeshift.com/v1/swap/rates?\
    sellAssetId=eip155:1/slip44:60&\
    buyAssetId=eip155:1/erc20:0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48&\
    sellAmountCryptoBaseUnit=1000000000000000000" \
      -H "X-API-Key: your-api-key"
    ```
  </Step>
  <Step title="Generate a quote">
    Pick the best rate and generate executable transaction data:
    ```bash
    curl -X POST "https://api.shapeshift.com/v1/swap/quote" \
      -H "X-API-Key: your-api-key" \
      -H "Content-Type: application/json" \
      -d '{
        "sellAssetId": "eip155:1/slip44:60",
        "buyAssetId": "eip155:1/erc20:0xa0b86991c6218b36c1d19d4a2e9eb0ce3606eb48",
        "sellAmountCryptoBaseUnit": "1000000000000000000",
        "receiveAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f2bD65",
        "swapperName": "THORChain",
        "sendAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f2bD65",
        "slippageTolerancePercentageDecimal": "0.005"
      }'
    ```
  </Step>
  <Step title="Submit on-chain">
    Use the returned \`steps[].transactionData\` object to submit via your wallet or web3 provider.
  </Step>
</Steps>

## Next Steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="lock" href="/getting-started/authentication">
    Learn about API key management and security best practices
  </Card>
  <Card title="Asset IDs" icon="tag" href="/getting-started/asset-ids">
    Understand the CAIP-19 format used for asset identification
  </Card>
</CardGroup>
